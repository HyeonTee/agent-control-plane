SQL migrations live here and are embedded in the Hub binary. Name each file
`000001_description.sql`, increment the number, and never edit an applied file.
The migration runner stores a SHA-256 digest for every applied migration.
